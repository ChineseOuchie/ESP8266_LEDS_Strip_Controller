String rootHtml() {
  return "<!DOCTYPE html> <html lang=\"en\"> <head> <meta charset=\"UTF-8\"> <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\"> <title>Night Lamp</title> <link rel=\"stylesheet\" href=\"https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css\"> <script src=\"https://cdn.jsdelivr.net/npm/@simonwep/pickr/dist/pickr.min.js\"></script> <link rel=\"stylesheet\" href=\"https://cdn.jsdelivr.net/npm/@simonwep/pickr/dist/themes/classic.min.css\"/> <script src=\"https://cdn.jsdelivr.net/npm/sweetalert2@10\"></script> <style> body{background-color: #34495e; } </style> </head> <body class=\"container d-flex align-items-center flex-column mt-5\"> <div class=\"card\"> <div class=\"card-body text-center d-flex justify-content-center align-items-center flex-column\"> <div id=\"color-picker-1\" class=\"mx-auto\"></div> </div> </div> <div class=\"mt-5\"> <div id=\"light-on\" class=\"btn btn-warning\">Light on</div> <div id=\"light-off\" class=\"btn btn-primary\">Light off</div> </div> <script> const pickr1 = new Pickr({ el: '#color-picker-1', default: \"#FFFFFF\", swatches: [ 'rgba(255, 255, 255, 1)', 'rgba(255, 0, 0, 1)', 'rgba(0, 255, 0, 1)', 'rgba(0, 0, 255, 1)', 'rgba(244, 67, 54, 1)', 'rgba(233, 30, 99, 1)', 'rgba(156, 39, 176, 1)', 'rgba(103, 58, 183, 1)', 'rgba(63, 81, 181, 1)', 'rgba(33, 150, 243, 1)', 'rgba(3, 169, 244, 1)', 'rgba(0, 188, 212, 1)', 'rgba(0, 150, 136, 1)', 'rgba(76, 175, 80, 1)', 'rgba(139, 195, 74, 1)' ], components: { preview: false, opacity: false, hue: true, interaction: { hex: true, rgba: true, input: true, save: true } } }); pickr1.on('save', (color, instance) => { const red = Math.round(color.toRGBA()[0]); const green = Math.round(color.toRGBA()[1]); const blue = Math.round(color.toRGBA()[2]); console.log(red, green, blue); const formdata = new FormData(); formdata.append('red', red); formdata.append('green', green); formdata.append('blue', blue); fetch('http://192.168.1.199/login', { method: 'POST', body: formdata }) .then(result => result.json()) .then(data => { const changeColorToast = Swal.mixin({ toast: true, position: 'bottom', showConfirmButton: false, timer: 2000, timerProgressBar: true }); changeColorToast.fire({ icon: 'success', title: data.message }); }); }); document.getElementById('light-on').addEventListener('click', () => { fetch('http://192.168.1.199/lightOn') .then(result => result.json()) .then(data => { const lightOnToast = Swal.mixin({ toast: true, position: 'bottom', showConfirmButton: false, timer: 2000, timerProgressBar: true }); lightOnToast.fire({ icon: 'success', title: data.message }); }); }); document.getElementById('light-off').addEventListener('click', () => { fetch('http://192.168.1.199/lightOff') .then(result => result.json()) .then(data => { const lightOffToast = Swal.mixin({ toast: true, position: 'bottom', showConfirmButton: false, timer: 2000, timerProgressBar: true }); lightOffToast.fire({ icon: 'success', title: data.message }); }); }); </script> </body> </html>";
}

